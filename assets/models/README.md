# Model weights

Default weights: `yolov8n.pt` (Ultralytics YOLOv8 nano).

Replace or override with a fine-tuned checkpoint via:

```bash
python run_detection.py --weights assets/models/your_model.pt
```
